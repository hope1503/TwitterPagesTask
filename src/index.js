import React from 'react';
import ReactDOM from 'react-dom';
import SearchApp from './SearchApp';
import App from './App';

ReactDOM.render(
  <App />,  //Edit as SearchApp to open up the search page on the server

  document.getElementById('root')
);
